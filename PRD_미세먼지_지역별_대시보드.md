# PRD — 오늘의 공기 (전국 시·도별 미세먼지)

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | **v0.2** — 열린 질문 5건 전부 확정, MVP 구현 완료 반영 |
| 작성일 | 2026-08-19 |
| 작성자 | 박선영 |
| 상태 | 확정 · MVP 구현 완료 (§15) |
| 서비스명 | **오늘의 공기** (repo: `today-air`) |
| 프로젝트 성격 | 개인 사이드 프로젝트 |
| 플랫폼 | 반응형 웹 (PC / 모바일 브라우저), 최소 폭 360px |
| 배포 | GitHub Pages + GitHub Actions |
| 스택 | 바닐라 ES 모듈 + CSS (빌드 도구 없음) |

### v0.1 → v0.2 변경 요약

| 항목 | v0.1 | v0.2 |
| --- | --- | --- |
| 데이터 경로 | 서버리스 프록시 (Vercel 가정) | **GitHub Actions 스케줄 수집 → 정적 JSON** |
| 프론트 스택 | React + Vite + TS + Tailwind | **바닐라 ES 모듈 + CSS** |
| 등급 색상 | "구현 단계에서 확정" | **확정** (§7.1) — 검증된 status 4단계 팔레트 |
| 지도 소스 | 미정 (Q4) | **통계청 SGIS 2018, 공공누리 1유형** |
| 기본 선택 | 서울 (잠정) | 서울 + URL 파라미터 · 마지막 조회 지역 복원 |

---

## 1. 배경 및 문제 정의

미세먼지 정보는 이미 여러 곳에서 제공되지만, 대부분 다음 중 하나의 문제를 갖는다.

- **내 지역만 보여준다.** 기상 앱 위젯은 현재 위치의 수치만 알려주므로, "지금 부산은 어떤가", "이번 주말 갈 강원도는 괜찮은가" 같은 **지역 간 비교**가 어렵다.
- **정보 밀도가 너무 높다.** 에어코리아 원본 사이트는 측정소 단위의 8개 항목을 한 번에 보여줘 일반 사용자가 "그래서 지금 나가도 되나"를 판단하기 어렵다.
- **탐색 비용이 크다.** 다른 지역을 보려면 드롭다운 선택 → 검색 → 페이지 이동을 거쳐야 한다.

본 서비스는 **"전국 어디가 좋고 어디가 나쁜지를 한 화면에서, 클릭 한 번으로"** 확인하는 데 집중한다.

### 왜 지금 만드는가 (사이드 프로젝트 관점)

- 공공데이터포털 API는 무료이고 인증 절차가 가벼워 개인 프로젝트로 완결 가능하다.
- 데이터 구조가 단순(17개 시·도 × 소수 지표)해 **디자인·인터랙션 품질에 시간을 쓸 수 있다.** 포트폴리오로서의 가치가 데이터 파이프라인 복잡도보다 UI 완성도에서 나온다.

---

## 2. 목표 (Goals)

| # | 목표 | 측정 방법 |
| --- | --- | --- |
| G1 | 진입 후 **3초 이내**에 전국 미세먼지 상황의 전체 그림을 파악할 수 있다 | 첫 화면에서 지도/랭킹이 스크롤 없이 보임 (LCP < 2.5s) |
| G2 | **클릭 1회**로 원하는 시·도의 상세 수치를 볼 수 있다 | 칩 → 상세 카드까지 인터랙션 1회 |
| G3 | 수치를 몰라도 **"오늘 어떤가"를 판단**할 수 있다 | 모든 수치에 등급 라벨·색상·행동 문구 동반 |
| G4 | 모바일에서도 데스크톱과 동등한 기능을 쓸 수 있다 | 360px 폭에서 전 기능 동작 |

### 비목표 (Non-Goals) — 이번 버전에서 하지 않는 것

- 회원가입·로그인, 서버 DB, 사용자 데이터 저장
- 푸시 알림 / 이메일 알림
- 시·군·구 및 개별 측정소 단위 상세 조회 *(→ v1.1 검토, §12)*
- 예보(내일/모레) 및 과거 이력 통계
- 오존·이산화질소 등 미세먼지 외 대기오염 항목의 전면 노출
- 위치 기반 자동 지역 감지 (GPS)

---

## 3. 타깃 사용자 및 핵심 시나리오

### 페르소나

**P1. 비교 탐색형 (주 타깃)**
주말 나들이·출장을 앞두고 여러 지역의 공기질을 견줘 보는 사람. "서울이랑 강원 중 어디가 나은가"가 궁금하다.

**P2. 빠른 확인형**
아침에 창밖이 뿌예서 "지금 우리 지역 나쁨인가"만 5초 안에 확인하고 싶은 사람.

### 핵심 시나리오

| ID | 시나리오 | 관련 기능 |
| --- | --- | --- |
| S1 | 접속하자마자 지도 색상만 보고 "오늘 전국이 대체로 보통이구나"를 안다 | F-04 |
| S2 | "경기" 칩을 눌러 PM10 42, PM2.5 21 / 보통 등급과 행동 요령을 확인한다 | F-01, F-02 |
| S3 | 랭킹에서 오늘 가장 공기 좋은 지역이 제주임을 확인하고, 제주를 눌러 상세를 본다 | F-03 → F-02 |
| S4 | 지도에서 빨갛게 칠해진 충남을 눌러 왜 나쁜지(수치) 확인한다 | F-04 → F-02 |

---

## 4. 정보 구조 및 화면 흐름

```
[진입]
   │
   ▼
┌──────────────────────────────────────────┐
│  헤더: 서비스명 / 기준시각 / 새로고침       │
├──────────────────────────────────────────┤
│  ① 시·도 칩 리스트 (17개)  ← F-01         │
│     [서울][부산][대구][인천] … [제주]      │
├──────────────────────────────────────────┤
│  ② 선택 지역 상세 카드      ← F-02        │
│     경기 · 보통                            │
│     PM10 42㎍/㎥   PM2.5 21㎍/㎥           │
│     "실외 활동에 큰 무리는 없어요"          │
├─────────────────────┬────────────────────┤
│  ③ 등급 색상 지도    │  ④ 전국 랭킹        │
│     ← F-04          │     ← F-03         │
│   (대한민국 17개     │   1. 제주  12 좋음  │
│    시·도 단색 채움)  │   2. 강원  18 좋음  │
│                     │   …                │
└─────────────────────┴────────────────────┘
```

**상태 동기화 원칙:** 칩 · 지도 · 랭킹은 **하나의 `selectedSido` 상태를 공유**한다. 어느 쪽에서 선택하든 나머지 둘의 선택 표시가 함께 갱신되고, 상세 카드가 교체된다. (S3, S4의 전제)

---

## 5. 기능 요구사항

### F-01. 시·도 칩 선택

| 항목 | 내용 |
| --- | --- |
| 설명 | 17개 시·도를 가로 스크롤 가능한 칩(chip) 리스트로 노출. 클릭 시 해당 지역 선택 |
| 우선순위 | **P0 (필수)** |

**요구사항**

- FR-01-1: 칩 순서는 **오늘 등급이 나쁜 순**이 아니라 **고정된 행정구역 순서**(서울→부산→…→제주)를 따른다. 순서가 매번 바뀌면 위치 기억이 무너져 재방문 시 탐색 비용이 커진다.
- FR-01-2: 각 칩은 **지역명 + 등급 색상 점(dot)** 을 함께 표시해, 칩 리스트만 훑어도 대략의 분포를 알 수 있게 한다.
- FR-01-3: 선택된 칩은 채움 배경 + 굵은 텍스트로 명확히 구분한다. (색상 대비 4.5:1 이상)
- FR-01-4: 진입 시 기본 선택값은 **서울**. 단 URL `?sido=` 파라미터가 있으면 그것을, 없고 이전 방문 기록이 있으면 마지막으로 본 지역을 우선한다.
- FR-01-5: 모바일에서 칩 리스트는 가로 스크롤하며, 스크롤 가능함을 알리는 페이드 마스크를 우측에 둔다.
- FR-01-6: 키보드 `←/→`로 칩 간 이동, `Enter`/`Space`로 선택 가능해야 한다.

---

### F-02. 선택 지역 상세 카드

| 항목 | 내용 |
| --- | --- |
| 설명 | 선택된 시·도의 미세먼지 수치·등급·행동 요령을 표시 |
| 우선순위 | **P0 (필수)** |

**표시 항목**

| 요소 | 내용 | 비고 |
| --- | --- | --- |
| 지역명 | 예: 경기 | |
| 종합 등급 | 좋음 / 보통 / 나쁨 / 매우나쁨 | PM10·PM2.5 중 **나쁜 쪽 등급**을 대표로 채택 |
| PM10 | 값(㎍/㎥) + 등급 | |
| PM2.5 | 값(㎍/㎥) + 등급 | |
| 행동 요령 | 등급별 한 문장 | 아래 표 참조 |
| 기준 시각 | "2026-08-19 14:00 기준" | API `dataTime` |
| 측정소 수 | "도내 32개 측정소 평균" | 신뢰도 근거 제시 |

**등급별 행동 요령 문구 (안)**

| 등급 | 문구 |
| --- | --- |
| 좋음 | 공기가 깨끗해요. 야외 활동하기 좋은 날입니다. |
| 보통 | 실외 활동에 큰 무리는 없어요. 민감하신 분은 무리한 운동만 피하세요. |
| 나쁨 | 외출 시 마스크를 챙기세요. 장시간 실외 활동은 줄이는 게 좋아요. |
| 매우나쁨 | 가급적 실외 활동을 피하고, 창문을 닫아 두세요. |
| 정보 없음 | 해당 지역의 측정값을 받아오지 못했어요. |

- FR-02-1: 등급은 **색상만으로 구분하지 않는다.** 반드시 텍스트 라벨을 병기한다. (색각 이상 접근성)
- FR-02-2: 값이 결측(`-`, `null`, 빈 문자열)이면 "—"로 표기하고 등급은 "정보 없음"으로 처리한다. **결측을 0으로 취급해 '좋음'으로 표시하는 것을 금지한다.**
- FR-02-3: 지역 전환 시 카드 내용은 페이드/슬라이드 전환하되, 카드 높이는 고정해 레이아웃 점프(CLS)를 막는다.

---

### F-03. 전국 랭킹 / 비교

| 항목 | 내용 |
| --- | --- |
| 설명 | 17개 시·도를 미세먼지 농도 기준으로 정렬해 순위로 표시 |
| 우선순위 | **P0 (필수)** |

**요구사항**

- FR-03-1: 기본 정렬 기준은 **PM2.5 오름차순(좋은 곳부터)** 으로 한다. 건강 영향이 더 큰 지표이며, "어디가 좋은지"를 찾는 것이 주 사용 목적이기 때문이다.
- FR-03-2: 정렬 기준 토글을 제공한다: `PM2.5` / `PM10`, `좋은 순` / `나쁜 순`.
- FR-03-3: 각 행은 `순위 · 지역명 · 수치 · 등급 배지 · 상대 길이 막대`로 구성한다. 막대는 17개 시·도 중 최댓값을 100%로 하는 상대 길이로 그린다.
- FR-03-4: 현재 선택된 지역의 행은 하이라이트해, 랭킹 안에서 "내 관심 지역이 몇 위인지"를 즉시 찾을 수 있게 한다.
- FR-03-5: 랭킹 행 클릭 시 해당 지역이 선택되고 상세 카드가 갱신된다. (S3)
- FR-03-6: 결측 지역은 순위에서 제외하고 리스트 **하단에 "정보 없음" 그룹**으로 모아 표시한다.
- FR-03-7: 767px 이하에서는 상위 5개만 노출하고 "전체 보기"로 확장한다.

---

### F-04. 등급별 색상 지도

| 항목 | 내용 |
| --- | --- |
| 설명 | 대한민국 17개 시·도 경계 지도를 등급 색상으로 채워 표시 (Choropleth) |
| 우선순위 | **P1 (MVP 포함, F-01~03 완료 후 착수)** |

**요구사항**

- FR-04-1: **인라인 SVG 기반**으로 구현한다. 외부 지도 타일(카카오·네이버·Google Maps)을 쓰지 않는다. → API 키 불필요, 오프라인 렌더 가능, 번들 경량, 색 채움이 자유로움.
- FR-04-2: 각 시·도 영역(`<path>`)에 `data-sido` 속성을 부여하고, 등급에 따라 `fill`을 적용한다.
- FR-04-3: 영역 클릭 시 해당 지역이 선택된다. (S4)
- FR-04-4: 선택된 영역은 굵은 외곽선(stroke)으로 표시한다. 채움 색은 등급을 나타내야 하므로 선택 표시에 색을 쓰지 않는다.
- FR-04-5: 데스크톱에서 hover 시 지역명 + 수치 툴팁을 띄운다. 터치 기기에서는 hover 대신 탭 즉시 선택으로 동작한다.
- FR-04-6: 세종·광역시 등 면적이 작은 지역은 클릭 타깃이 44×44px 미만이 되지 않도록, 지도 외부에 별도 라벨/보조 칩을 배치하거나 최소 타깃 영역을 덧씌운다.
- FR-04-7: 지도 옆에 **등급 범례(legend)** 를 항상 표시한다.
- FR-04-8: 지도는 장식이 아닌 정보이므로, `<title>`/`aria-label`로 각 영역에 "경기, 보통, PM2.5 21"을 읽어 줄 수 있어야 한다.

---

### F-05. 데이터 갱신 및 상태 표시

| 우선순위 | **P0 (필수)** |
| --- | --- |

- FR-05-1: 헤더에 **기준 시각**(`dataTime`)과 **마지막 조회 시각**을 구분해 표시한다. 둘은 다른 개념이며, 이를 혼동시키면 "데이터가 안 바뀐다"는 오해를 낳는다.
- FR-05-2: 수동 새로고침 버튼을 제공한다.
- FR-05-3: 자동 갱신은 **10분 주기**로 한다. 원본 데이터가 1시간 단위로 갱신되므로 그보다 잦은 폴링은 무의미하고 API 호출 한도만 소모한다.
- FR-05-4: 탭이 백그라운드일 때는 폴링을 중단하고, 다시 포커스되면 즉시 1회 조회한다.
- FR-05-5: 상태별 UI를 모두 정의한다.
  - **로딩:** 스켈레톤(칩·카드·랭킹 자리 유지). 스피너 단독 사용 금지.
  - **에러:** "데이터를 불러오지 못했어요" + 재시도 버튼 + 마지막 성공 데이터가 있으면 그것을 흐리게 유지하고 "10분 전 데이터" 배지 표시.
  - **부분 결측:** 전체 실패로 처리하지 않고, 해당 지역만 "정보 없음"으로 표시.

---

## 6. 데이터 명세

### 6.1 사용 API

**한국환경공단\_에어코리아\_대기오염정보** (공공데이터포털, 무료·활용신청 필요)

| 항목 | 값 |
| --- | --- |
| Base URL | `https://apis.data.go.kr/B552584/ArpltnInforInqireSvc` |
| Operation | `getCtprvnRltmMesureDnsty` (시도별 실시간 측정정보 조회) |
| 인증 | `serviceKey` (발급받은 인증키, URL 인코딩 주의) |
| 갱신 주기 | **1시간** (매시 정각 이후 순차 반영) |
| 호출 한도 | 개발계정 기준 **일 500회** (운영계정 승인 시 확대) |

### 6.2 요청 파라미터

| 파라미터 | 값 | 설명 |
| --- | --- | --- |
| `serviceKey` | 발급 키 | 필수 |
| `returnType` | `json` | JSON 응답 |
| `sidoName` | **`전국`** | ⭐ 핵심 설계 결정 — 아래 참조 |
| `numOfRows` | `1000` | 전국 측정소 약 600여 개를 1페이지에 수용 |
| `pageNo` | `1` | |
| `ver` | `1.3` | PM2.5(`pm25Value`) 및 항목별 등급 포함 버전 |

> ⭐ **설계 결정: `sidoName=전국`으로 1회만 호출한다.**
>
> 이 API는 시·도 평균이 아니라 **측정소 단위 원시 데이터**를 반환한다. 시·도별 값이 필요하다고 해서 17개 시·도를 각각 호출하면 **화면 1회 갱신에 17회 호출**이 발생해 일 500회 한도를 30회 갱신 만에 소진한다. `전국` 1회 호출로 전 측정소를 받아 **애플리케이션에서 시·도별로 집계**하면, 랭킹(F-03)과 지도(F-04)에 필요한 17개 값이 **호출 1회**로 모두 확보된다. 10분 폴링 기준 하루 144회로 한도 내에 안정적으로 들어온다.

### 6.3 주요 응답 필드

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `sidoName` | string | 시·도명 (예: `경기`) |
| `stationName` | string | 측정소명 |
| `dataTime` | string | 측정 기준 시각 `YYYY-MM-DD HH:mm` |
| `pm10Value` | string | 미세먼지 농도 (㎍/㎥) |
| `pm25Value` | string | 초미세먼지 농도 (㎍/㎥) |
| `pm10Grade` | string | PM10 등급 `1`~`4` |
| `pm25Grade` | string | PM2.5 등급 `1`~`4` |
| `khaiValue` / `khaiGrade` | string | 통합대기환경지수 및 등급 |
| `pm10Flag` / `pm25Flag` | string | 결측 사유 (`점검및교정`, `자료이상`, `통신장애` 등) — **값이 있으면 해당 측정값은 신뢰 불가** |

### 6.4 시·도 대표값 산정 규칙

- **DR-1:** 시·도 대표값 = 해당 시·도 측정소들의 **유효 값 산술 평균**(소수 첫째 자리 반올림).
- **DR-2:** 유효 값의 조건 — `pmXXValue`가 숫자로 파싱되고, `pmXXFlag`가 비어 있을 것. 둘 중 하나라도 위배되면 평균 계산에서 제외한다.
- **DR-3:** 시·도 내 유효 측정소가 **0개**면 그 시·도는 "정보 없음"으로 처리한다.
- **DR-4:** 등급은 API가 준 측정소별 `grade`를 평균하지 않는다. **평균 농도를 구한 뒤, 그 농도를 등급 기준표에 대입해 재산정**한다. (등급의 평균은 의미가 없다.)
- **DR-5:** `dataTime`은 측정소마다 다를 수 있다. 헤더 기준 시각은 전체 측정소 중 **최빈값**을 사용한다.

### 6.5 등급 기준표 (에어코리아 기준)

| 등급 | PM10 (㎍/㎥) | PM2.5 (㎍/㎥) |
| --- | --- | --- |
| 좋음 | 0 ~ 30 | 0 ~ 15 |
| 보통 | 31 ~ 80 | 16 ~ 35 |
| 나쁨 | 81 ~ 150 | 36 ~ 75 |
| 매우나쁨 | 151 이상 | 76 이상 |

### 6.6 시·도명 목록 (API 파라미터 값)

`서울` `부산` `대구` `인천` `광주` `대전` `울산` `세종` `경기` `강원` `충북` `충남` `전북` `전남` `경북` `경남` `제주` (+ 전체 조회용 `전국`)

> ⚠️ API가 쓰는 값은 **축약형**(`서울`)이고, 화면 표기·SVG 지도 경로 ID는 정식 명칭(`서울특별시`)을 쓰는 경우가 많다. **매핑 테이블을 단일 상수 파일로 관리**하고, 지도 path ID와 API 값이 어긋나 특정 지역만 색이 안 칠해지는 상황을 초기에 점검한다.

---

## 7. 디자인 시스템

### 7.1 등급 색상 (확정)

미세먼지 등급은 **순서가 있는 심각도(ordinal severity)** 이므로, 임의의 카테고리 색이 아니라 검증된 4단계 status 팔레트를 쓴다. 배지 글자색은 각 배경에서 4.5:1 이상을 확보하도록 짝지었다.

| 등급 | 배경 | 글자색 | 대비 | 아이콘 |
| --- | --- | --- | --- | --- |
| 좋음 | `#0ca30c` | `#0b0b0b` | 5.87 | ● |
| 보통 | `#fab219` | `#0b0b0b` | 10.73 | ◆ |
| 나쁨 | `#ec835a` | `#0b0b0b` | 7.46 | ▲ |
| 매우나쁨 | `#d03b3b` | `#ffffff` | 4.80 | ■ |
| 정보 없음 | `#898781` | `#0b0b0b` | 5.48 | ? |

기타 토큰 — 표면 `#fcfcfb` / `#1a1a19`, 배경 `#f9f9f7` / `#0d0d0d`, 본문 잉크 `#0b0b0b` / `#ffffff`, 보조 `#52514e` / `#c3c2b7`, 흐린 글자 `#898781`(양쪽), 실선 `#e1e0d9` / `#2c2c2a`.

> ⚠️ **알려진 한계:** "보통"(`#fab219`)과 "나쁨"(`#ec835a`)은 인접 색상 판별 기준을 아슬아슬하게 통과하지 못한다(정상 시야 ΔE 13.6, 기준 15). 색만으로 두 등급을 구분하도록 두지 않는 것이 필수 완화 조건이며, 아래 DS-2·DS-4 가 그 장치다.

- **DS-1:** 라이트/다크 두 모드 모두 별도로 검증된 값을 쓴다. 다크 모드는 라이트 값의 자동 반전이 아니다.
- **DS-2:** 등급 표현에는 항상 **색 + 텍스트 라벨(+ 아이콘)** 을 함께 쓴다. 적록색약 사용자가 좋음/보통/나쁨을 구분하지 못하는 상황을 만들지 않는다.
- **DS-3:** 지도 채움 색과 선택 표시를 모두 색으로 처리하지 않는다. 채움=등급, 외곽선=선택.
- **DS-4:** 지도 위에 **수치를 직접 표기**한다. 지도만 놓고 보아도 색에 의존하지 않고 값을 읽을 수 있어야 한다. 아울러 같은 정보를 전국 순위 목록이 표 형태로 중복 제공한다 (DS-1 한계에 대한 완화 장치).

### 7.2 반응형 브레이크포인트

| 구간 | 레이아웃 |
| --- | --- |
| ~ 767px (모바일) | 1열 세로 스택: 칩 → 상세 카드 → 지도 → 랭킹(상위 5 + 더보기) |
| 768 ~ 1023px (태블릿) | 1열, 지도·랭킹은 전체 폭 |
| 1024px ~ (데스크톱) | 상단 칩/카드 전체 폭, 하단 2열 (좌: 지도 / 우: 랭킹) |

최소 지원 폭 **360px**.

---

## 8. 기술 아키텍처

### 8.1 스택 (확정)

- **프론트엔드:** 바닐라 ES 모듈 + 순수 CSS — 빌드 도구·의존성 없음
- **지도:** 인라인 SVG (통계청 SGIS 2018 시·도 경계를 단순화해 생성)
- **데이터 수집:** Node 20 스크립트 (의존성 없음, 전역 `fetch` 사용)
- **호스팅:** GitHub Pages (`main` 브랜치 루트)
- **스케줄러:** GitHub Actions (cron)

> **왜 빌드리스인가.** GitHub Pages 는 정적 호스팅이므로 어차피 산출물이 정적 파일이다. 이 규모(17개 지역 × 지표 2종, 화면 3개)에서 React·Vite·Tailwind 를 얹으면 얻는 것보다 관리 비용이 크다. `npm install` 없이 파일만 올리면 돌아가고, 의존성 취약점 알림에 시달릴 일도 없다.

### 8.2 ⚠️ 반드시 먼저 해결해야 할 두 가지 제약

**(1) API 키 노출**
브라우저에서 `apis.data.go.kr`을 직접 호출하면 `serviceKey`가 네트워크 탭과 소스에 그대로 드러난다. 개인 프로젝트라 해도 키를 도용당하면 **일일 호출 한도가 타인에 의해 소진**되어 서비스가 죽는다.

**(2) CORS**
공공데이터포털 API는 브라우저 교차 출처 요청을 위한 CORS 헤더를 보장하지 않는다. 로컬 개발에서는 프록시로 우회되지만, **배포 후 프로덕션에서만 실패**하는 전형적인 함정이다.

GitHub Pages 에는 서버리스 함수를 둘 수 없으므로, 두 문제를 **데이터 수집 시점을 옮겨서** 해결한다.

**→ 해결: 데이터 수집을 스케줄된 CI 로 옮기고, 브라우저는 정적 JSON 만 읽는다.**

```
GitHub Actions (매시 25분, cron)
   │   SERVICE_KEY 는 GitHub Secrets 에만 존재 — 저장소·번들 어디에도 없음
   ▼
apis.data.go.kr  ──►  scripts/fetch-air.mjs  ──►  data/air.json 커밋
                       · sidoName=전국 으로 1회 호출
                       · 시·도별 유효 측정값 평균 산출
                                                       │
                                                       ▼
                                     브라우저 ── 같은 오리진의 정적 JSON fetch
                                                (CORS 요청 자체가 발생하지 않음)
```

- **TR-1:** 브라우저에 전달되는 것은 원시 측정소 600여 건이 아닌 **집계된 17건**이다. 응답이 수십 분의 1로 줄어 모바일 로딩이 빨라지고, §6.4의 집계 규칙이 한 파일에 모인다.
- **TR-2:** 원본 갱신 주기가 1시간이므로 수집도 시간당 1회면 충분하다. **일 24회** 호출로 한도(500회) 대비 큰 여유를 둔다.
- **TR-3:** 수집 실패 시 스크립트는 **0이 아닌 코드로 종료하고 파일을 건드리지 않는다.** 워크플로가 커밋을 건너뛰므로 **직전 성공 데이터가 그대로 서비스된다.** 공공 API는 간헐적 장애가 잦아 이 폴백이 체감 안정성을 좌우한다.
- **TR-4:** 유효 시·도가 9개 미만이면 부분 장애로 보고 마찬가지로 기존 파일을 지킨다. 일부 지역만 비어 있는 데이터로 덮어쓰는 사고를 막는다.
- **TR-5:** 클라이언트는 `fetchedAt` 을 현재 시각과 비교해 **2시간을 넘으면 "오래된 데이터" 배지**를 띄운다. 수집이 며칠째 멈춰 있는데 사용자가 모르는 상황을 만들지 않는다.
- **TR-6:** 수집 스크립트는 외부 패키지를 쓰지 않는다. Actions 러너에서 `npm install` 단계가 없어 실행이 빠르고, 공급망 리스크가 없다.

**이 구조가 포기하는 것:** 사용자가 새로고침을 눌러도 원본을 실시간으로 다시 부르지는 못한다(정적 JSON 재조회만 한다). 원본이 시간당 1회 갱신이므로 실질 손해는 없다고 판단했다.

### 8.3 데이터 스키마 (`data/air.json`)

```json
{
  "dataTime": "2026-08-19 14:00",
  "fetchedAt": "2026-08-19T05:25:11.000Z",
  "stationCount": 612,
  "source": "한국환경공단 에어코리아 대기오염정보 (공공데이터포털)",
  "regions": {
    "서울": { "pm10": 42, "pm25": 21, "stations": 40, "pm10Stations": 40, "pm25Stations": 39 }
  }
}
```

- **TR-7:** JSON 은 **농도 숫자만** 담고 등급은 담지 않는다. 등급 판정은 클라이언트의 `config.js` 한 곳에서만 이뤄지므로, 기준이 바뀌어도 고칠 곳이 하나다.
- **TR-8:** 결측은 `null` 이다. **0 이 아니다.** 이 구분이 무너지면 데이터 없는 지역이 "좋음"으로 표시된다 (FR-02-2).

---

## 9. 비기능 요구사항

| ID | 항목 | 기준 |
| --- | --- | --- |
| NFR-1 | 초기 로딩 | LCP 2.5초 이내 (3G Fast 기준) |
| NFR-2 | 상호작용 | 칩/지도/랭킹 클릭 → 상세 반영 100ms 이내 (이미 받은 데이터를 재사용, 추가 네트워크 요청 없음) |
| NFR-3 | 접근성 | WCAG 2.1 AA — 명도 대비 4.5:1, 키보드 전체 조작, 지도·차트 대체 텍스트 |
| NFR-4 | 브라우저 | 최신 Chrome / Safari / Edge / Firefox 및 iOS Safari, Android Chrome |
| NFR-5 | API 호출량 | 원본 API 호출 **일 300회 이하** 유지 (한도 500회 대비 여유 확보) |
| NFR-6 | 오류 내성 | 원본 API 장애 시에도 마지막 데이터로 화면이 유지되어야 함 |

---

## 10. 성공 지표

사이드 프로젝트인 만큼, 트래픽 목표보다 **완성도 기준**을 지표로 삼는다.

| 구분 | 지표 | 목표 |
| --- | --- | --- |
| 완성도 | Lighthouse Performance / Accessibility | 각 90점 이상 |
| 완성도 | 정의된 모든 상태(로딩·에러·부분결측·정상)의 UI 구현 | 100% |
| 안정성 | 배포 후 7일간 프록시 5xx 비율 | 1% 미만 |
| 사용성 | 지인 5명 대상 테스트에서 "지금 어디가 제일 좋은지" 안내 없이 찾기 | 5/5 성공 |
| 부가 | 재방문 시 첫 화면에서 원하는 지역을 찾기까지 걸린 시간 | 5초 이내 |

---

## 11. 구현 순서 (권장)

각 단계가 끝날 때마다 **동작하는 화면**이 남도록 쪼갰다.

1. **데이터 확보** — 프록시 함수 + 시·도 집계 로직 + 등급 산정. 브라우저에서 JSON이 뜨는 것까지.
2. **F-01 + F-02** — 칩 리스트와 상세 카드. 이 시점에 이미 "쓸 수 있는 서비스"가 된다.
3. **F-05** — 로딩/에러/결측 상태와 갱신 로직.
4. **F-03** — 랭킹.
5. **F-04** — SVG 지도. (가장 손이 많이 가므로 마지막. 여기서 막혀도 1~4로 완결된 서비스가 남는다.)
6. 반응형 마감 → 접근성 점검 → 배포.

---

## 12. 향후 확장 (Out of Scope, 아이디어 보관)

- 시·군·구 / 측정소 단위 드릴다운 (시·도 칩 → 하위 지역)
- 24시간 추이 그래프
- 대기오염 예보 API 연동 (내일·모레 등급)
- 즐겨찾기 지역 (localStorage)
- 지역 간 직접 비교 모드 (2개 지역 나란히)
- OG 이미지 동적 생성 → SNS 공유 시 오늘의 전국 지도 미리보기

---

## 13. 결정 기록 (구 열린 질문)

| ID | 질문 | 결정 | 근거 |
| --- | --- | --- | --- |
| **Q1** | 배포 위치 | **GitHub Pages** | 서버리스 프록시를 못 쓰는 대신, 수집을 GitHub Actions 로 옮겨 키 노출·CORS 를 함께 해소 (§8.2) |
| **Q2** | 기본 선택 지역 | **서울**. 단 URL `?sido=` 파라미터 > 마지막 조회 지역(localStorage) > 서울 순으로 우선 | 첫 방문은 빈 화면 없이 채우고, 재방문자는 관심 지역이 복원되게 |
| **Q3** | 모바일 랭킹 노출 | **상위 5개 + "전체 보기"** | 세로 스크롤 절약. 768px 이상에서는 전체 노출 |
| **Q4** | SVG 지도 소스 | **통계청 SGIS 2018 시·도 경계** (공공누리 제1유형) | 상업적 이용·변형 허용, 출처 표시만 필요. 17개 시·도 정확히 포함(세종 포함) |
| **Q5** | 서비스 이름 | **오늘의 공기** (repo `today-air`) | — |
| **Q6** | 프론트 스택 (신규) | **바닐라 ES 모듈 + CSS** | 이 규모에서 빌드 체인의 관리 비용이 이득보다 큼 (§8.1) |

---

## 14. 남은 리스크

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| GitHub Actions 무료 cron 은 정시에 안 돌 수 있음 (수 분 ~ 수십 분 지연) | 데이터가 최대 1시간 이상 늦을 수 있음 | 원본이 시간당 갱신이라 실질 영향 작음. 2시간 초과 시 "오래된 데이터" 배지 (TR-5) |
| 인증키 만료·한도 초과 | 수집 중단, 데이터 정지 | 워크플로 실패 알림 + 화면의 오래된 데이터 배지로 감지 |
| "보통"과 "나쁨" 색상 근접 | 지도에서 두 등급 혼동 | 수치 직접 표기 + 순위 목록 중복 제공 (DS-4) |
| 리포지토리에 매시 커밋이 쌓임 | 히스토리 비대 | 변경 없으면 커밋 생략. 필요 시 주기적 squash |
| 강원·전북 명칭 변경(특별자치도) | 표기 불일치 | API 는 축약형(`강원`)을 계속 사용. 표시용 정식 명칭은 `config.js` 매핑에서 관리 |

---

## 15. 구현 현황 (2026-08-19)

MVP 전 범위 구현 완료. 검증 항목:

| 항목 | 결과 |
| --- | --- |
| 등급 판정 경계값 테스트 | 20건 통과 (구간 경계 ±1, null/NaN 포함) |
| 집계 로직 통합 테스트 (가짜 API 응답) | 10건 통과 (flag 결측 제외, `-`·빈 문자열 처리, 미정의 시도명 무시, dataTime 최빈값) |
| 반응형 렌더 확인 | 360 / 390 / 768 / 1280px — 가로 오버플로 없음 |
| 라이트·다크 모드 | 양쪽 렌더 확인, 색상 픽셀 검증 |
| 상태별 UI | 정상 / 오류(404) / 결측 지역 / 선택 상태 확인 |

**미구현(의도적):** §12 향후 확장 항목 전부.

---

## 부록. 참고 자료

- 한국환경공단\_에어코리아\_대기오염정보 — 공공데이터포털
- 에어코리아 통합대기환경지수(CAI) 등급 기준
- 통계청 SGIS 행정구역 경계 (공공누리 제1유형)
- WCAG 2.1 Level AA 명도 대비 기준
